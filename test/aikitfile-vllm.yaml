#syntax=aikit:test
apiVersion: v1alpha1
debug: true
runtime: cuda
backends:
  - vllm
config: |
  - name: opt-125m
    backend: vllm
    parameters:
      model: "facebook/opt-125m"
    trust_remote_code: true
